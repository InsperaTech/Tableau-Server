try:
    # Create the project folder in a single line
    new_project = server.projects.create(TSC.ProjectItem(name=archive_project_name))
 
    print(f"Project folder '{archive_project_name}' created in site '{site_item.name}'.")
except TSC.ServerResponseError as e:
    # Check for 409001 (Resource Conflict) and handle the error more generically
    if e.code == '409001':
        print(f"Project folder '{archive_project_name}' already exists or a conflict occurred.")
    else:
        print(f"Error creating project folder: {e}")
        # You can log the full response for more details if needed
        print(e.content)
        # Handle the error as needed

has context menu
